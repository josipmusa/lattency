group = dev.lattency
version = 0.1.0

# The IntelliJ Platform this plugin is developed and tested against.
platformVersion = 2025.2.6.2

# The oldest supported IDE build. untilBuild is deliberately left open (see
# build.gradle.kts) and its honesty is enforced by `verifyPlugin` in CI.
pluginSinceBuild = 252

javaVersion = 21

org.gradle.configuration-cache = true
org.gradle.caching = true
org.gradle.jvmargs = -Xmx2g

# Filled in by the repository owner; used for changelog links and the plugin page.
pluginRepositoryUrl = https://github.com/josipmusa/lattency
